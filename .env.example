# The Day 1 backend runs with checked-in fixtures only.
DEMO_MODE=1

# Person 3 will define the supported provider/model values before live use.
MODEL_PROVIDER=
MODEL_NAME=
MODEL_API_KEY=
MODEL_TIMEOUT_SECONDS=30

# Upload parsing is not integrated in the Day 1 walking skeleton.
MAX_DEMO_SIZE_MB=256
